node_modules/
# Persistent browser profile used to hold the Gourmet Glatt bot-check clearance and
# selected-store cookies between runs. Machine-local, never committed.
.gg-profile/
.npgs-profile/

# Transient batch-submission state. data/match-cache.json holds the durable
# decisions and IS committed — that's what makes matching a one-time cost.
data/match-batch.json
